import { UserApi } from '../../services/userapi';
import { FluxConstants } from './constants';

import { User } from '../../models/user';

export default class Actions {

    static sendRequest(email: string, password: string) {

    }
};

